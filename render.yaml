services:
  # Backend API Service
  - type: web
    name: caregiver-chatbot-backend
    env: docker
    region: oregon
    plan: starter
    healthCheckPath: /health
    envVars:
      - key: OPENAI_API_KEY
        sync: false
    buildCommand: docker build -t backend .
    startCommand: docker run -p $PORT:8000 backend

  # Frontend Web Service
  - type: web
    name: caregiver-chatbot-frontend
    env: docker
    region: oregon
    plan: starter
    healthCheckPath: /
    buildCommand: docker build -f caregiver-chatbot/Dockerfile -t frontend caregiver-chatbot/
    startCommand: docker run -p $PORT:80 frontend
    envVars:
      - key: REACT_APP_API_URL
        value: https://caregiver-chatbot-backend.onrender.com 